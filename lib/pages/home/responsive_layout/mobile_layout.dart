import 'package:flutter/material.dart';

class MobileBody extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("This is Mobile Layout", style: TextStyle(fontSize: 20,)),
    );
  }
}