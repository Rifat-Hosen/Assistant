import 'package:flutter/material.dart';

class TabletBody extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("This is Tablet Layout", style: TextStyle(fontSize: 24)),
    );
  }
}